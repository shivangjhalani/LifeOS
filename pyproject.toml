[project]
name = "groq-whisper-transcription-experiment"
version = "0.1.0"
description = "Minimal Groq Whisper transcription experiment"
requires-python = ">=3.10"
dependencies = [
  "groq",
  "python-dotenv",
]
